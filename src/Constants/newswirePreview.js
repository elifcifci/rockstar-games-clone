import {newswireConstants} from "./newswire"

export const newswirePreviewConstant = [];
for (let previewCount = 0; previewCount <= 5; previewCount++) {
    newswirePreviewConstant.push(newswireConstants[previewCount])
}